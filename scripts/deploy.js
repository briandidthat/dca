const { ethers } = require("hardhat");
const { chamberFactoryFixture } = require("../test/utils");

async function main() {

}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
